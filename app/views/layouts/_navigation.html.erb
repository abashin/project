


<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
  <h5 class="my-0 mr-md-auto font-weight-normal">myraspiska.ru</h5>
  <nav class="my-2 my-md-0 mr-md-3">

    <%= if check_admin
          link_to 'Все расписки', '/all', :class => "p-2 text-dark"
        end %>
    <%= if check_admin
          link_to 'Все пользователи', '/index', :class => "p-2 text-dark"
        end%>
      <%= link_to 'Новая расписка', '/user_debt_main_page', :class => "p-2 text-dark"
          if logged_in?
            link_to 'Новая расписка', '/debt_for_borrower', :class => "p-2 text-dark"

          end %>


  </nav>

  <%= if logged_in?
        link_to 'Личный кабинет', '/personal_area', :class => "btn btn-outline-primary"
      else
        link_to 'Вход', '/signin', :class => "btn btn-outline-primary"
      end %>
</div>